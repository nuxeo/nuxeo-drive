name: test
on:
  push:
    branches:
      - Getwindows-Secret
  workflow_dispatch:
jobs:
  your_job:
    runs-on: ubuntu-latest
    steps:
      - name: Example Step
        env:
          YOUR_SECRET: ${{ secrets.DOCKER_USERNAME }}
        run: |
          echo "$YOUR_SECRET" > certificate_macOS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: my-artifact
          path: certificate_macOS.txt
